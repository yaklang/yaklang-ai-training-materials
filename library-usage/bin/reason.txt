未完成原因：与 `facades` 和 `simulator` 库一样，在循环遍历数组时遇到了无法解决的 Yaklang 编译器语法错误。

主要难点：
1.  语法问题：这似乎是一个普遍存在的问题，表明我对 Yaklang 的循环迭代语法理解有误，或者该语法有未知的限制。
2.  功能复杂性：`bin` 库用于处理二进制数据流。要进行有意义的测试，需要构造精确的、复杂的字节数组作为输入，并对解析结果进行逐字节的验证。这需要对目标二进制格式有深入的了解，测试脚本编写成本很高。
