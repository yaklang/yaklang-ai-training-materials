未完成原因：在测试脚本的后期调试中，遇到了与变量作用域和上下文传递相关的运行时错误（YakVM Panic），且多次尝试修复均未成功。

主要难点：
1.  运行时错误：`ExecuteAsExpression` 方法在传递变量作用域时，行为与预期不符，导致断言失败。例如，沙箱内无法访问通过参数传入的变量，或者对外部变量的隔离测试失败。
2.  作用域复杂性：`sandbox` 库的核心就是隔离执行环境。要彻底验证其安全性，需要设计多种复杂的场景来测试边界情况，例如嵌套沙箱、全局变量污染、库函数覆盖等，这对测试脚本的设计要求很高。
