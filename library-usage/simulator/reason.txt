未完成原因：与 `facades` 库类似，在尝试编写测试脚本时遇到了无法解决的 Yaklang 循环语法错误。

主要难点：
1.  语法问题：多种 `for` 循环的尝试都失败了，这表明在迭代数组并断言其内部元素类型时，Yaklang 解释器有特殊的语法要求，目前尚未掌握。
2.  环境依赖：`simulator` 库是一个浏览器自动化工具，强依赖于本地安装的浏览器环境（如 Chrome）和对应的 WebDriver。在没有图形界面的服务器或 Docker 环境中进行测试非常困难且不稳定。
