未完成原因：同样遇到了无法解决的 Yaklang `for` 循环语法错误。

主要难点：
1.  语法问题：在遍历配置函数列表时，标准的循环语法均告失败，阻碍了测试脚本的编写。
2.  功能复杂性：`openapi` 库用于解析 OpenAPI/Swagger 规范文件。一个完整的测试需要提供多种版本的规范文件（v2, v3），并验证其能否正确生成 HTTP 请求模型。这需要准备多种测试数据，并对复杂的输出结构进行断言。
