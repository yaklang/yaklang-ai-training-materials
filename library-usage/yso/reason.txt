未完成原因：用户反馈仅检查函数存在性的测试没有意义，而进行深入的功能性测试存在困难。

主要难点：
1.  环境依赖：`yso` 库用于生成 Java 反序列化 Payload。要验证 Payload 的有效性，需要一个可受攻击的 Java 环境来接收和执行这些 Payload，并在该环境中验证结果（如命令是否执行、文件是否创建）。
2.  功能验证复杂性：测试脚本本身无法判断生成的字节流是否是一个有效的、可利用的 Java 对象。这需要与外部 Java 进程进行复杂的交互，超出了 Yaklang 脚本测试的常规范围。
