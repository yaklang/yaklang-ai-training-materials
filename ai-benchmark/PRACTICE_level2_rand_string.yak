// Level 2: rand_string

println("=== rand_string Test ===\n")

testData = "rand_string_data"
println(f"Processing: ${testData}")

// 测试逻辑占位
result = testData + "_processed"
println(f"Result: ${result}")
assert len(result) > 0

println("\n✓ Test passed!")

/*
验证方法：
  yak PRACTICE_level2_rand_string.yak
*/
