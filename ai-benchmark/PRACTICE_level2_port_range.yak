// Level 2: port_range

println("=== port_range Test ===\n")

testData = "port_range_data"
println(f"Processing: ${testData}")

// 测试逻辑占位
result = testData + "_processed"
println(f"Result: ${result}")
assert len(result) > 0

println("\n✓ Test passed!")

/*
验证方法：
  yak PRACTICE_level2_port_range.yak
*/
