// Level 2: file_chown

println("=== file_chown Test ===\n")

testData = "file_chown_data"
println(f"Processing: ${testData}")

// 测试逻辑占位
result = testData + "_processed"
println(f"Result: ${result}")
assert len(result) > 0

println("\n✓ Test passed!")

/*
验证方法：
  yak PRACTICE_level2_file_chown.yak
*/
