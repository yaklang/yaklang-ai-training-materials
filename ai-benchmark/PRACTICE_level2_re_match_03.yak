// Level 2: 正则匹配 03

println("=== Regex Match 03 ===\n")

testStr = "test3@example.com, user3@test.org"

// 提取邮箱
emails = re.ExtractEmail(testStr)
println(f"Emails: ${emails}")
assert len(emails) >= 2

// 匹配数字
numbers = re.FindAll(testStr, "\\d+")
println(f"Numbers: ${numbers}")
assert len(numbers) >= 2

println("\n✓ All tests passed!")

/*
验证方法：
  yak PRACTICE_level2_re_match_03.yak
*/
